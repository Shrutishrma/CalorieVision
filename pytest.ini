[pytest]
# Register custom markers so pytest doesn't warn about unknown ones
markers =
    network: marks tests that make real network calls (deselect with -m "not network")
    real_video: marks tests that require a real workout video in shared/test-videos/

# Default test paths (directories now use underscores — no workaround needed)
testpaths =
    tests
    cv_pipeline/eval
    cv_pipeline/motion
    fusion_pipeline/segmentation
    fusion_pipeline/ocr
    fusion_pipeline/fusion

# Asyncio mode (for pytest-asyncio)
asyncio_mode = strict
